{
  "name": "Friendship",
  "type": "object",
  "properties": {
    "requester_id": {
      "type": "string",
      "description": "ID of user who sent friend request"
    },
    "addressee_id": {
      "type": "string",
      "description": "ID of user who received friend request"
    },
    "status": {
      "type": "string",
      "enum": [
        "pending",
        "accepted",
        "blocked"
      ],
      "default": "pending",
      "description": "Status of the friendship"
    }
  },
  "required": [
    "requester_id",
    "addressee_id"
  ]
}
